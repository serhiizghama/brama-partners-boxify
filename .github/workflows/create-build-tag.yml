name: 'Test Trigger on Main'

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test-job:
    name: 'Run Test Job'
    runs-on: ubuntu-latest
    steps:
      - name: 'Say Hello'
        run: echo "Hello, World! This test workflow was triggered successfully on main."
